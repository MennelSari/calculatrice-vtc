
export interface Day {
  name: string;      // Nom du jour
  coefficient: number; // Coefficient multiplicateur pour ce jour
  target: number;     // Objectif journalier calculé
  actual: number;     // Montant réellement gagné
} 